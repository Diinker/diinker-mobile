# -------------------------------------------------------------------
# PROJECT DEPENDENCIES
# -------------------------------------------------------------------

# Node modules
/node_modules

# Lock files (if you use both npm and yarn, ignore one to avoid conflicts)
# package-lock.json # UNCOMMENT to ignore npm lock file (usually you commit this)
# yarn.lock         # UNCOMMENT to ignore yarn lock file (usually you commit this)


# -------------------------------------------------------------------
# EXPO / REACT NATIVE BUILD & CACHE
# -------------------------------------------------------------------

# Expo build cache and local state
.expo/

# Directory generated for web builds
web-build/

# Generated files for prebuild/EAS Build (if using managed workflow)
# NOTE: If you are running a bare/EAS Build setup, you might want to 
# keep the top-level 'android' and 'ios' directories, but ignore their build output.

# Android build outputs
android/build/
android/.gradle/
android/app/build/
android/app/.cxx/
android/local.properties

# iOS build outputs
ios/build/
ios/DerivedData/
ios/Pods/
ios/.xcode.env.local

# Temporary files created by Metro for health checks
.metro-health-check*


# -------------------------------------------------------------------
# ENVIRONMENT / SECRETS
# -------------------------------------------------------------------

# Local environment files (which should not be committed)
.env
.env.local
.env.*.local

# NOTE: If you use a tool like 'expo-secure-store' or inject environment 
# variables through `app.config.js` for EAS Build, you do not need 
# to worry about your API keys being committed. However, if you have 
# private files like `google-services.json`, add them here.
# google-services.json
# GoogleService-Info.plist


# -------------------------------------------------------------------
# LOGS AND DEBUGGING
# -------------------------------------------------------------------

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# -------------------------------------------------------------------
# OS and IDE files
# -------------------------------------------------------------------

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Visual Studio Code
.vscode/
# Add specific .vscode files if you don't want local launch configurations
# .vscode/settings.json
# .vscode/launch.json
# .vscode/tasks.json

# JetBrains IDEs (WebStorm, IntelliJ)
.idea/